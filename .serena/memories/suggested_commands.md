Useful commands for this repo:\n- `cargo fmt --check`\n- `cargo clippy --all-targets -- -D warnings`\n- `cargo test`\n- Focused tests: `cargo test --test compile <name>`, `cargo test --test sema <name>`, `cargo test --test weights <name>`, or `cargo test <unit_test_name>`\n- Run docs build: `mdbook build` in `docs/`\n- Security checks: `cargo audit`, `cargo deny check`\n- Common CLI flows: `cargo run --quiet -- import <model.onnx> -o <model.nnl> --weights-dir <dir>`, `cargo run --quiet -- inspect <model.nnl>`, `cargo run --quiet -- compile <model.nnl> --emit exe|lib|obj|shared|header -o <output>`\n- Linux shell utilities available: `git`, `ls`, `rg`, `make`, `cc`, `c++`, `python3`, `curl`.